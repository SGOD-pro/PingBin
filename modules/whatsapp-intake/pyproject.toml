[project]
name = "whatsapp-intake"
version = "1.0.0"
description = "Twilio WhatsApp Serverless Webhook Ingestion & SQS Queue Dispatcher"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
