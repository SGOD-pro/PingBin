[project]
name = "warehouse-recycling-logistics"
version = "1.0.0"
description = "Post-Cleanup Warehouse Routing & Materials Recovery (MRF) Logistics Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
